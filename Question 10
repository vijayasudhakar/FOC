#include <stdio.h>

int main() {

    int n;

    printf("Enter n: ");

    scanf("%d", &n);

    int sum = n * (n + 2) * (n + 1) / 3;

    printf("Sum of squares of even numbers from 2 to %d is %d\n", n, sum);

    return 0;

}
