#include<stdio.h>
int main()
{
    int n,i,sum;
        printf("enter the number to find its sum");
        scanf("%d",&n);
        for (i=1;i<n;i++)
        {
            sum=sum+i;
            }
            printf("sum of natural number upto %d is %d:",n,sum);
            }
